let completeInfoCards = document.querySelectorAll(".ui.complete-info-card");

for (let card of completeInfoCards) {
	  let flex = card.querySelector(".flex");
	  let info = flex.querySelector(".info");
}
